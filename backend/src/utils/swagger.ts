// Swagger integration removed per project preference.
// This file remains as a harmless stub to avoid import errors if any
// references remain elsewhere during a transitional refactor.
import express from "express";

export function setupSwagger(_app: express.Express) {
  // no-op: swagger has been intentionally removed
}

export default null;
